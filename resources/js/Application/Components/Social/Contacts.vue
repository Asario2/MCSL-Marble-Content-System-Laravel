<template>
    adas
</template>
<!-- <script>
methods:{
emitSelection() {
  const selectedNames = this.contacts
    .filter(c => this.selectedIds.includes(c.id))
    .map(c => c.name);

  this.$emit('update:selectedNames', {
    source: 'contact',
    names: selectedNames,
    formatted: selectedNames.map(name => `{${name}}`).join(', '),
    count: selectedNames.length,
  });
}
};
</script> -->

